// Namespace
var App=(function(module){
    module.views={};
    module.models={};
    module.collections={};
    module.config={};

    // ---- App Configs --------
    module.config.validationOn = false;

    return module;
})(App || {});